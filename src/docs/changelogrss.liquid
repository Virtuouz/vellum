---
title: 'Changelog Feed'
summary: 'A feed of all our changelog posts'
permalink: '/changelog/feed.xml'
layout:
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xmlns:media="http://search.yahoo.com/mrss/">
  <title>{{ title }}</title>
	<subtitle>{{ summary }}</subtitle>
	<link href="{{ site.url }}{{ permalink }}" rel="self"/>
	<link href="{{ site.url }}/"/>
	<updated>{{ collections.changelog | getNewestCollectionItemDate | dateToRfc3339 }}</updated>
	<id>{{ site.url }}</id>
	{% for post in collections.changelog reversed%}
    {% capture bookshopContent %}
      {% if post.data.editorial_blocks %}
        {% bookshop_include "doc" editorial_blocks: post.data.editorial_blocks %}
      {% endif %}
    {% endcapture %}
    {% capture absolutePostUrl %}{{ site.url }}{{ post.url | url }}{% endcapture %}
    <entry>
      <title>{{ post.data.title }}</title>
      <link href="{{ absolutePostUrl }}"/>
      <updated>{{ post.date | dateToRfc882 }}</updated>
      <id>{{ absolutePostUrl }}</id>
      <author>
        <name>{{ site.name }}</name>
      </author>
      {% assign image = post.data.socialImage | default: site.socialImage %}
      {% if image and image != "" %}
      <media:content medium="image" url="{{site.url}}{{ image  }}" />
      {% endif %}
      <content type="html"><![CDATA[{{ bookshopContent | append: post.templateContent | renderTransforms | sanitizeRss }}]]></content>
    </entry>
	{% endfor %}
</feed>